#include<bits/stdc++.h>
using namespace std; 
int getMostVisited(int n,int sprints[])
{
    int counter[n];
    int i=0;
    int arraySize=sizeof(sprints)/sizeof(sprints[0]);
    while(i<arraySize)
    {
        if(sprints[i]<sprints[i++])
        {
            for(int i=sprints[i];i<)
        }
    }

}
int main(int argc, char const *argv[])
{
     
    return 0;
}
 